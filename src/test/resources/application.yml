spring:
  datasource:
    url: jdbc:h2:mem:testdb
    data: data-test.sql
    initialization-mode: always
  shell:
    interactive:
      enabled: false
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: false